import { sqliteTable, text, integer, uniqueIndex, index } from 'drizzle-orm/sqlite-core';
export const players=sqliteTable('players',{id:text('id').primaryKey(),phoneKey:text('phone_key').notNull(),name:text('name').notNull(),createdAt:integer('created_at').notNull()},t=>[uniqueIndex('players_phone_unique').on(t.phoneKey)]);
export const sessions=sqliteTable('sessions',{tokenHash:text('token_hash').primaryKey(),userId:text('user_id').notNull().references(()=>players.id),expiresAt:integer('expires_at').notNull()},t=>[index('sessions_expiry').on(t.expiresAt)]);
export const limits=sqliteTable('rate_limits',{key:text('key').primaryKey(),count:integer('count').notNull(),expiresAt:integer('expires_at').notNull()});
export const challenges=sqliteTable('challenges',{phoneKey:text('phone_key').primaryKey(),nextSendAt:integer('next_send_at').notNull(),expiresAt:integer('expires_at').notNull(),verificationSid:text('verification_sid')});
export const periods=sqliteTable('periods',{id:text('id').primaryKey(),kind:text('kind').notNull(),periodKey:text('period_key').notNull(),endsAt:integer('ends_at').notNull(),finalizeAfter:integer('finalize_after').notNull(),finalizedAt:integer('finalized_at'),prizeRules:text('prize_rules')},t=>[index('periods_finalization').on(t.finalizedAt,t.finalizeAfter)]);
export const runs=sqliteTable('runs',{id:text('id').primaryKey(),userId:text('user_id').notNull().references(()=>players.id),game:text('game').notNull(),day:text('day').notNull(),week:text('week').notNull(),requestKey:text('request_key').notNull(),startedAt:integer('started_at').notNull(),submitBy:integer('submit_by').notNull(),config:text('config').notNull(),status:text('status').notNull(),score:integer('score'),endReason:text('end_reason'),endedAt:integer('ended_at'),trace:text('trace'),traceHash:text('trace_hash'),displayName:text('display_name').notNull(),rejection:text('rejection')},t=>[uniqueIndex('runs_one_per_day').on(t.userId,t.game,t.day),uniqueIndex('runs_start_idempotency').on(t.userId,t.requestKey),index('runs_day_scores').on(t.game,t.day,t.status,t.score),index('runs_week_scores').on(t.game,t.week,t.status,t.score),index('runs_expiry').on(t.status,t.submitBy)]);
export const standings=sqliteTable('standings',{periodId:text('period_id').notNull().references(()=>periods.id),userId:text('user_id').notNull().references(()=>players.id),name:text('name').notNull(),score:integer('score').notNull(),rank:integer('rank').notNull(),runId:text('run_id').notNull().references(()=>runs.id)},t=>[uniqueIndex('standings_period_user').on(t.periodId,t.userId)]);
