import { handle,json,user,ranking,finalize,HttpError,db } from '../../../lib/server';
import { boundaries } from '../../../lib/engine';
export async function GET(request:Request){return handle(request,async()=>{await finalize();const url=new URL(request.url),period=url.searchParams.get('period')||'today';if(!['today','week'].includes(period))throw new HttpError('Invalid leaderboard.');const b=boundaries(Date.now()),u=await user(request),key=url.searchParams.get('key')||(period==='today'?b.day:b.week);if(!/^\d{4}-\d{2}-\d{2}$/.test(key))throw new HttpError('Invalid period.');const p=await db().prepare('SELECT finalized_at FROM periods WHERE id=?').bind(period+':'+key).first<{finalized_at:number|null}>();if(p?.finalized_at){const rows=await db().prepare('SELECT user_id id,name,score,rank FROM standings WHERE period_id=? ORDER BY rank,user_id LIMIT 50').bind(period+':'+key).all();const me=u?await db().prepare('SELECT user_id id,name,score,rank FROM standings WHERE period_id=? AND user_id=?').bind(period+':'+key,u.id).first():null;return json({rows:rows.results,me,finalized:true,key});}return json({...await ranking(period,key,u?.id||null),finalized:false,key,...b});});}
